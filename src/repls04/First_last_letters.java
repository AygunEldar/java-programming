package repls04;
import  java.util.*;

public class First_last_letters {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        String[] words = {input.nextLine(), input.nextLine(), input.nextLine(), input.nextLine(), input.nextLine()};

        // String words1 = "hello";
        //TODO: Write your code below
        for (int i = 0; i < words.length; i++) {
            //String words1 = words[i];
            //  char[] letters=words1.toCharArray();
            //int charCount =words1.length();
            // letters =words1.charAt(0)+""+words1.charAt(charCount-1);
            // System.out.print(words1.charAt(0)+words1.charAt(charCount-1));
//            System.out.println(Arrays.toString(words[i].substring(0, 1) +  words[i].charAt(words[i].length() - 1));
           // String  word2 =words[i].substring(0, 1) +  words[i].charAt(words[i].length() - 1);
           // System.out.print(String.join(", ",word2));
           // String [] words3 = new String(word2);
           // System.out.print(Arrays.toString(words3));
        }
        // System.out.print(letters.charAt(0)+""+letters.charAt(charCount-1));


        // System.out.print(Array.toString(each));

    }

}
